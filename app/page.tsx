import path from "path";

export default async function Page() {
  const base = '/vercel/path0/contentsx4/hersteller'
  const secondBase = base // If we remove this line, the project deploys successfully.
  return (
    <div>
      <h1>TEST at 24/10/08</h1>
    </div>
  );
}
